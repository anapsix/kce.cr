name: kce
version: 0.5.0
homepage: https://github.com/anapsix/kce.cr

description: |
  CLI tool and library to parse KUBECONFIG, and generate new config for specific context.

executables:
  - kce

authors:
  - Anastas Dancha <anapsix@random.io>

targets:
  kce:
    main: src/cli.cr

license: MIT
